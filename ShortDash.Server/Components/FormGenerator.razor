<EditForm EditContext="@EditContext" OnValidSubmit="@(context => OnValidSubmit.InvokeAsync(context))">
    <DataAnnotationsValidator />
    <ValidationSummary />
    @foreach (var property in properties)
    {
        @RenderFormElement(property)
    }
</EditForm>