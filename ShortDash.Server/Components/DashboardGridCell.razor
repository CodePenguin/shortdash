@namespace ShortDash.Server.Components
@using Data

@if (Cell.CellType == DashboardCellType.Action)
{
    <ActionButton Cell="Cell">
        <DashboardGridButton Cell="Cell" />
    </ActionButton>
}
else if (Cell.CellType == DashboardCellType.DashLink)
{
    <DashboardLinkButton Cell="Cell">
        <DashboardGridButton Cell="Cell" />
    </DashboardLinkButton>
}
else
{
    <div class="grid-icon">
        &nbsp;
    </div>
}

@code {
    [Parameter]
    public DashboardCell Cell { get; set; }
}
