<div class="grid container-fluid">
    <div class="row text-center">
        @foreach (var cell in DashboardCells)
        {
            <div class="col py-3 px-3 flex-grow-0">
                <DashboardGridCell Cell="cell" OnMoveCellLeft="MoveCellLeft" OnMoveCellRight="MoveCellRight" OnRemoveCell="RemoveCell" EditMode="EditMode" />
            </div>
        }
        @if (EditMode)
        {
            <div class="col py-3 px-3 flex-grow-0">
                <a href="" @onclick="ShowAddDialog" @onclick:preventDefault>
                    <div class="add-cell grid-icon rounded">
                        <div class="grid-icon-content d-table-cell align-middle">
                            <span class="fas fa-plus"></span>
                        </div>
                    </div>
                </a>
            </div>
        }
    </div>
</div>